The tail command is used to print last 10 lines of a file by default.

$ tail FILE_NAME                                            (display only the last 10 lines of the file)
$ tail -n NO FILE_NAME                                      (prints the last NO lines instead of last 10 lines)
  tail -n 3 FILE_NAME
  tail -3 FILE_NAME
$ tail -v FILE_NAME
$ tail FILE_NAME FILE_NAME
$ tail -q FILE_NAME FILE_NAME
$ tail -f /FOLDER_NAME/FILE_NAME
$ tail -f /FOLDER_NAME/FOLDER_NAME/FILE_NAME.EXTENSION
  tail -f /opt/sonarqube/logs/sonar.log

$ sudo tail -f /FOLDER_NAME/FOLDER_NAME/FILE_NAME.EXTENSION
  sudo tail -f /opt/sonarqube/logs/sonar.log


tail & sort with pipe - The 'tail & sort' command is generally used with pipe (|).

$ tail -n NO FILE_NAME | sort
  tail -n 6 FILE_NAME | sort 
