The command to make a Executable files.
$ chmod +x FILE_NAME
$ chmod +x FILE_NAME.EXTENSION

Executable files are usually stored in one of several standard directories on the hard disk drive (HDD) on Unix like operating systems.
$ printenv PATH    (OR)    $ echo $PATH
/bin
/sbin
/usr/bin
/usr/sbin
/usr/local/bin
/usr/local/sbin

So move an Executable files in a specific path location.
$ mv FILE_NAME /bin
$ mv /FOLDER_NAME/FILE_NAME /bin
$ mv FOLDER_NAME/FILE_NAME /usr/local/bin/
$ mv FOLDER_NAME/FILE_NAME /usr/local/bin/.
$ mv -v FILE_NAME /usr/local/bin/
$ mv -v FILE_NAME:original /usr/local/bin/FILE_NAME:rename
